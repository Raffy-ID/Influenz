<?xml version="1.0" encoding="utf-8"?>
<ScrollView
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/scrollViewPasswordChanged"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:fillViewport="true">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:gravity="center"
        android:padding="16dp">

        <!-- Ikon sukses (pastikan drawable 'ic_check_circle' tersedia di folder res/drawable) -->
        <ImageView
            android:id="@+id/ivSuccess"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:src="@drawable/ic_check_circle"
            android:contentDescription="@string/success_icon_desc" />

        <!-- Judul konfirmasi password telah diubah -->
        <TextView
            android:id="@+id/tvPasswordChangedTitle"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="@string/kata_sandi_diubah"
            android:textSize="28sp"
            android:textStyle="bold"
            android:gravity="center"
            android:layout_marginTop="20dp" />

        <!-- Pesan tambahan (opsional) -->
        <TextView
            android:id="@+id/tvPasswordChangedMessage"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="@string/password_anda_telah_berhasil_diubah"
            android:textSize="16sp"
            android:gravity="center"
            android:layout_marginTop="10dp" />

        <!-- Jeda ruang -->
        <Space
            android:layout_width="match_parent"
            android:layout_height="30dp" />

        <!-- Tombol untuk kembali ke Login -->
        <Button
            android:id="@+id/btnLogin"
            android:layout_width="match_parent"
            android:layout_height="50dp"
            android:text="@string/login"
            android:background="@drawable/button_rounded_pu"
            android:textColor="@android:color/white"
            android:layout_marginTop="20dp" />

    </LinearLayout>
</ScrollView>
