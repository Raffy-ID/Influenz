<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/Influenz2"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Influenz2"
        tools:targetApi="31">
        <activity
            android:name=".SplashActivity"
            android:exported="true"
            android:theme="@style/Theme.Influenz2">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Deklarasi activity lainnya -->
        <activity android:name=".OnboardingActivity" android:theme="@style/Theme.Influenz2"/>
        <activity android:name=".LoginActivity" android:theme="@style/Theme.Influenz2"/>
        <activity android:name=".SignUpActivity" android:theme="@style/Theme.Influenz2"/>
        <activity android:name=".ForgotPasswordActivity" android:theme="@style/Theme.Influenz2"/>
        <activity android:name=".ChangePasswordActivity" android:theme="@style/Theme.Influenz2"/>
        <activity android:name=".EmailSentActivity" android:theme="@style/Theme.Influenz2" />
        <activity android:name=".PasswordChangedActivity" android:theme="@style/Theme.Influenz2"/>
        <activity android:name=".HomeActivity" android:theme="@style/Theme.Influenz2"/>
        <activity android:name=".ClassesActivity" android:theme="@style/Theme.Influenz2"/>
        <activity android:name=".ProductsActivity" android:theme="@style/Theme.Influenz2"/>
        <activity android:name=".ChatActivity" android:theme="@style/Theme.Influenz2"/>
        <activity android:name=".ProfileActivity" android:theme="@style/Theme.Influenz2"/>

    </application>

</manifest>